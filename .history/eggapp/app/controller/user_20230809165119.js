const { Controller } = require("egg");

class UserController extends Controller{

  /**
   * 获取用户列表
   */
  const { crx } = this;
  //临时数据
const mockUsers = [
    email:'1@qq.com',password:'123',registrationDate:'2021',role:'123',is_deleted:'1'
  ]
}
